import { Button } from '@/components/ui/button'
import React from 'react'

const CTA: React.FC = () => {
    return (
        <Button>
            Buy Now
        </Button>
    )
}

export default CTA
